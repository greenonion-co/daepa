export enum USER_ROLE {
  USER = 'user',
  BREEDER = 'breeder',
  ADMIN = 'admin',
}

export enum USER_STATUS {
  PENDING = 'pending',
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  SUSPENDED = 'suspended',
  DELETED = 'deleted',
}

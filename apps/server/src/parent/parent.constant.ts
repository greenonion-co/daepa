export type PARENT_ROLE = 'father' | 'mother';

export enum PARENT_STATUS {
  PENDING = 'pending',
  APPROVED = 'approved',
  REJECTED = 'rejected',
  DELETED = 'deleted',
  CANCELLED = 'cancelled',
}
